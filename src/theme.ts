import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#D4E157',
      light: '#f0f4c3',
    },
    secondary: {
      main: '#2C3639',
      light: '#3F4E4F',
    },
  },
});

export default theme;
